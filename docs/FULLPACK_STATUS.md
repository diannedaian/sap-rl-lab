# 完整包进度

更新：2026-09-18。完整Tier1–6、60只宠物的三模型平均学习型对手十胜率80%目标已通过：
独立测试82.27%（2,468/3,000），固定新种子确认81.87%（2,456/3,000），可靠性合格。
训练及评估均已结束，旧模型保留。范围是声明的冻结对手池，不是真实竞技场胜率。
结果、模型清单、步数、曲线和限制见`FULLPACK_DELIVERY.md`。以下保留各阶段历史状态。

## 已完成

- 新版60只非token宠物、18种普通食物及召唤/perk规则，隔离在`fullpack`命名空间。
- 最终完整回归：1,030项通过；新规则与编排定向测试60项。
- 最终封存代码分别在Tier5、Tier6跑过6,000组战斗双重复现和160局精确回放。
- 两阶段分别完成32步真实KL-PPO、保存重载、4局逐行验证，以及学习型对手快照检查。
- Tier5→Tier6迁移小测试：64个相同输入上的动作概率及价值预测完全一致，权重保存后
  重载一致。只改变环境契约并使用新优化器，不是把旧环境模型伪装成完整包成绩。
- 网络仍为两个64×64的Tanh MLP（actor/critic）；输入扩展后共341,516个参数，139种动作。
  没有扩大隐藏层、取消动作mask或改变奖励来追分。

## 训练历史（各条为当时状态，最终结果见页首）

`runs/fullpack-tier5-h40-v1`已结束：A/B完成原预算，学习型验证十胜率36.5%/39%；
C在183万步触发旧的可靠性早停，终点29%，较早合格检查点22%。该轮所有验证无截断。

`runs/fullpack-tier5-h40-long1-v1`三条谱系已各完成1,048,576新增步，均正常预算结束。
三支验证选中模型的学习型十胜率为50% / 45.5% / 47%，均值47.5%；各自最高家族
强制率5% / 1.5% / 2%，选中验证均无整局截断。初始验证逐行复现父检查点；未开测试。
这是Tier5课程结果，不是Tier6全开放成绩。

现已启动`runs/fullpack-tier6-h40-v1`（启动PID33906），开始构建Tier6全开放的数据和
独立对手池。完整回归1,023项通过，40回合迁移的64个输入推理一致性检查通过。
之后三支候选接续课程权重，沿用40回合、每步−0.005、KL0.01；普通段预算结束自动
追加训练，安全异常则明确记录。80%测试筛查通过后还需独立确认，不能自动算目标完成。
具体配方与停止条件见`FULLPACK_TIER6_CAMPAIGN.md`。当前尚无Tier6候选验证成绩。

启动验证：数据/示范已完成，阶段协议封存通过；首两支独立对手生成器已完成BC20轮
并进入真实PPO更新，均已超过60,000步，更新日志确认KL target=0.01。
进程检查确认两个worker存活、各占约一CPU核。这是在训练新对手，不是候选已达80%。

后续检查：首两支对手生成器均完成各1,048,576步且正常预算结束；数值型对手池已落盘，
召唤型在生成快照，第三支节奏型已自动接续。没有发现失败记录。
最终固定新对局的确认流程已实现，见`FULLPACK_CONFIRMATION40.md`；确认、编排与
持续学习停止逻辑的18项定向测试通过。它尚未执行正式确认，也不代表已达80%。

最新：7/7支对手均完成固定PPO预算及快照池。三支候选的Tier5→Tier6权重迁移、
每支64个输入上的动作概率/价值精确一致及保存重载检查全部通过。首段候选A/B已
启动（PID35901/35902），C排队；已确认Tier6、40回合、KL0.01和新优化器权重热启动。
新环境训练前验证基线：A学习型十胜率38.75%、B44.25%，各自最差家族强制率4%/2%，
均无整局截断且满足初始选模要求。两者随后已发生真实PPO更新；这不是训练后成绩，
也不能与Tier5的不同对手池成绩直接作升降结论。完整三模型结果和80%目标仍待验证。

首段后续：A/B各2,097,152步均正常完成；验证选中的终点十胜率分别69.75%/72%，
两者各自最差家族强制率均1%，无整局截断。B在中途出现14%强制率但后来恢复，
不合格检查点没有作为交付候选。C已自动启动（PID36687）并实际更新PPO，
完整包训练前基线35.5%、最高强制率2%、无截断。三模型平均与独立测试尚未产生，
不能用A/B两支成绩替代完整目标。C完成后正常续开下一段，不重新请求普通续训授权。

首段现已全部完成：三支各2,097,152步，验证选择的学习型十胜率为69.75% / 72% /
63.5%，平均68.42%。所有验证无整局截断；尚未打开候选测试成绩。
第二段已自动启动（A/B PID37468/37469，C排队），各再增加1,048,576步。
A/B起点逐局精确复现上一终点验证，随后均已发生超过50,000步真实PPO更新。
配方、对手池与交付门未改；首段结果留存，80%目标仍未完成。
跨段曲线工具`scripts/plot_fullpack_progress.py`已实现并用真实记录核对，
`runs/fullpack-tier6-curves-v2`是C仅训练到105万步时的历史快照，不是首段最终曲线。

第二段已全部完成：各再增1,048,576步，跨段验证选择为76.5% / 72.5% / 66.75%，
均值71.92%，高于首段68.42%；仍未开候选测试，也未达到目标。
第三段已自动启动（A/B PID38270/38271，C排队），各再增加1,048,576步，
依预先配方从第二段终点权重、新优化器继续，而不是回滚到验证最高检查点。
起点逐局精确复现检查通过，A/B已实际更新PPO。所有较早合格检查点继续保留用于选模。

第三段已全部正常完成：跨段验证选择76.5% / 75.5% / 72.75%，均值74.92%。
C本段合格最好成绩提升明显（66.75%→72.75%），A/B增益较小且有波动。
第四段已自动启动（A/B PID39970/39971，C排队），沿用全部既定配置；
起点精确重载验证后已实际更新PPO。尚未打开候选测试，80%目标没有完成。

第四段已全部正常完成：跨段验证选择83.75% / 79.5% / 75.5%，均值79.58%。
不能把79.58%四舍五入当作达标；尚未触发候选独立测试。
第五段已自动启动（A/B PID40975/40976，C排队），初始验证精确重载通过并已真实PPO更新。
全部奖励、KL0.01、冻结对手池、40回合与模型选择规则保持不变，继续各追加1,048,576步。

5%仍用于严格选交付模型，不再将略高于5%连续两次直接当成停止学习的理由。
严重持续退化的预先固定停止条件见`FULLPACK_CONTINUE.md`。旧合格权重和所有结果保留。
这一段预算用完不等于整体完成，也不需要再申请一次普通续训授权；之后推进Tier6全开放。
目前仍未达到完整包80%，不能把Tier5验证分数或某支最强模型作为目标完成证据。

## 40回合修正与恢复记录

原30回合训练因一局正常长局触顶而停止，并非已证明策略崩坏。用户授权把整局计算上限
改为40；官方介绍未写30回合封顶。原失败种子在40回合设置下第34回合自然达到10胜。
同族200局中199局结果完全不变，截断从1降为0。

- 新增40回合边界、时间输入缩放和真实PPO重载测试；完整回归1,012项通过。
- 新运行`runs/fullpack-tier5-h40-v1`已经启动，A/B恢复训练，C排队。
- A从131万步、B从最新保存的104万步续训；C按原BC配方起步，分别补齐原209万步预算。
- 整局40回合；每回合购物30步、奖励与KL0.01不变。只使用旧冻结对手池和验证集，未开测试。
- 后续Tier6训练也应使用`fullpack.horizon40.configuration`，不能退回历史30回合默认值。
- 细节见`FULLPACK_HORIZON40.md`。旧目录、旧模型、旧协议哈希复核均未改变。

## 首段记录（30回合，现已停止）

`runs/fullpack-tier5-v1`已于2026-09-17 14:38:58 UTC启动，初始状态为收集合法对局
与BC示范，不把这段准备时间称为PPO更新。过程日志在同级`.launch.log`。

启动检查：数据准备完成，协议/旧模型哈希复核通过。前两支对手生成器已经完成BC20轮，
验证动作准确率分别70.4%和70.5%（不是游戏胜率）；随后已实际进行至少67,584与61,440
个PPO决策，更新日志确认KL target=0.01且早停约束生效。其余生成器与候选仍在队列中。

- Tier5普通商店课程，Tier6宠物可作为升级奖励出现；不是最终全开放测试。
- 3类老师的1,800局训练示范、300局验证示范，包含失败局；各模型BC20轮。
- 7个独立学习型对手生成器各1,048,576 PPO步；3支候选各2,097,152步。
- 本段上限13,631,488个PPO决策；最多两个本机CPU进程、每模型一线程。
- 沿用KL target 0.01、每动作−0.005、30动作强制开战等recipe。
- 候选每262,144步验证；连续两次明显退化/可靠性不合格可停止。课程阶段不开测试分数。

本段跑完后，检查课程结果并迁移到Tier6、重建该环境下的训练/验证/测试对手池。
完整包未到80%且没有崩坏时可继续等预算加步；最终还需固定模型的独立确认评估。
当前尚无新版完整包的正式十胜率，不能将旧版82.1%算作新版结果。

## 证据与限制

- `runs/fullpack-rules-gate-v1/pytest.log`：完整测试真实输出。
- `runs/fullpack-stress-tier5-v1`、`runs/fullpack-stress-tier6-v1`：压力与回放证据。
- `runs/fullpack-training-smoke-tier5-v1`、`runs/fullpack-training-smoke-tier6-v1`：训练接口证据。
- `runs/fullpack-transfer-smoke-v1/transfer.json`：迁移与输入/输出契约证据。
- `FULLPACK_RULES.md`：来源、取整/优先级等客户端实证不足的边界；测试通过不等于官方同局一致。

旧模型/结果未覆盖，无上传、发布、删除或GPU作业。
