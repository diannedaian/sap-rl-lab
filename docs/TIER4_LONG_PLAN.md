# Tier4续训一段：预先固定的三支等预算实验

2026-09-15，用户批准继续训练；不是自动训练到80%，不扩宠物或改规则。

- 三个起点一律用上轮54101/54201/54301的固定2,097,152步终点，
  不按已经看过的独立测试分数挑模型；保留所有旧结果和失败记录。
- 每支新增1,048,576步，总新增3,145,728步；每条模型谱系累计3,145,728步。
  旧BC已完成，不重做；按既有续训方式复制完整actor/critic权重，重建优化器，
  新采样seed55101/55201/55301。这是权重热启动，不是恢复旧Adam/RNG的精确续跑。
- 保留原小MLP、全部环境规则、训练对手池及等权混合、动作mask、每动作−0.005、
  30动作强制开战、LR3e-4、gamma1、GAE0.95、entropy0、clip0.2、10epochs、
  8环境×256、batch256、KL target0.01；不同时修改奖励、网络或KL强度。
- 仍每262,144步验证，每族200局，原验证seed59000000，5个族。
  按原学习型优先规则选模，所有族强制局率≤5%、无截断、基本玩法及回归检查不改。
  保留验证不合格的终点用于诊断，但不将它冒充通过筛选的模型。
  若某支始终无合格检查点，selected为null，继续评估其他预定角色，不再令报告卡死。
- 比较每支续训起点、固定终点及实际验证选模；相同检查点以别名去重。
  训练初始验证必须与旧终点的同seed逐局记录完全一致；所有选定权重精确重载后开测试。
- 本轮测试episode seed68000000，每族500局，最多9独特模型/22,500测试局；
  这是既有独立对手池的新对局，不是新对手类型，也不是从未被研究过的生态。
  不用之前60000000那批测试局选检查点；本轮测试不选模或调整超参。
- 主比较：三支固定终点相对各自起点的学习型十胜率变化，逐支及均值报告；
  同时报强制局率、截断、不买宠物零胜失败与验证选模覆盖率，不只报最好一支。
  5%是操作性筛选线，不是“6%即完全训崩”的科学断言。
- 本机CPU最多2训练/评估worker；单模型1小时、全流程2小时硬上限；异常停止，
  无自动追加或重跑。自动保存曲线、检查点、验证/测试逐局记录与最多27个诊断回放。
- 新目录`runs/tier4-long-v1`；不覆盖旧模型，不上传，不改旧封存源文件，不自动交付。

研究问题：再给同一配方一段预算，强度是否继续增长、可靠性是否改善？
不是证明已经收敛、不是保证80%、也不是纯时间效应的随机对照（优化器/RNG重新开始）。
