# KL 中断后：共同检查点评估修订

2026-09-13。用户说明刚更新了Codex并授权继续。原`kl-brake-v1`保持失败状态和全部证据，不改写原预注册，不重训，不假称六个模型都完成209.7万步。

原流水线约47分50秒后报告worker非零退出；5个模型完成2,097,152步，control-204201只记录到1,839,104步，最近持久化权重为1,835,008步。数值/截断失败记录未见；原stdout会话已不可读，具体退出原因未被原日志保存。更新导致输出通道中断是推测，不作确定根因结论。

## 修订的固定比较

选择六模型全部具备的最大共同已保存检查点：**1,835,008追加步**。这是按文件可用性机械确定的中断后修订，且已经查看过验证曲线，不是原预注册的完整固定预算结论。
主要比较为这六个共同步数模型；次要比较为原ValidationGuard在0至1,835,008步的前缀上选出的合格检查点；不使用之后的验证分数选择。另包含三个初始与历史最佳，共最多16个比较。

模型/数据先复制到新的`runs/kl-brake-recovery-v1`。校验48个前缀检查点与19,200条已保存验证局、三对初始权重/配置，所有比较一并冻结，再逐模型做4族×100局精确验证重载。全部重载匹配后才做5族×200局测试；沿用原未开启的测试seed34,000,000，验证seed33,000,000。不用测试成绩重选、改参数或替换交付。

不追加任何PPO/BC决策；只新增历史最佳400局验证、最多6,400局重载和16,000局测试、共同步数模型最多18个诊断回放。最多两CPU worker、Torch单线程，恢复评估上限2小时。每worker完整stdout/stderr落盘；保存退出码；失败不自动重试，保留部分证据。

报告三对学习型对手十胜率、强制率、购买/冻结/换位、空买失败、剩余金币、截断与回退覆盖；解释实际梯度步数与KL是否下降、共同步数相比初始是否提升，区分原全预算失败和修订评估完成。保留原完整两对末尾验证结果仅作补充，不与修订测试混算。

纽约时间今天09:00给晨报，到时不完整也如实报告。40宠物、商店Tier1–3及升级奖励Tier4；已研究对手池的新局seed不是新生态泛化证据。不发布、不推送、不清理、不覆盖旧模型。
