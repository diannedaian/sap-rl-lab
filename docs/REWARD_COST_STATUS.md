# 奖励成本对照：训练、测试和复核完成

**最新结论：B 救回两个空队失败种子，但仍循环，未通过候选筛查。**
见[简明结果](REWARD_COST_REPORT.md)。程序会话 80842 已正常退出；回放审查会话
45142 已正常退出。以下保留启动记录，不代表仍在训练。

用户于 2026-09-08 批准 [预案](REWARD_COST_PLAN.md) 的额外预算。
上一轮报告中“待批准、尚未启动”是当时状态，不再是本轮阻塞条件。
上一轮负结果和所有存档保持原样。

- 输出：`runs/reward-cost-pilot-v1`。
- 协议 SHA256：`764d6ba30720595d5b263771fb5c675b64ec9c7bd5e17f0175b33d306b83a45a`。
- 本轮仅增加 12,582,912 次训练决策；三个种子 17301、203101、203201，
  每种子 A 每动作 −0.005，对 B 仅换位 −0.005，各从零 2,097,152 步。
- 两臂探索系数都是 0；最多两个 CPU 工作者，不改变模型、对手混合或游戏规则。
- 新增九项测试，全套 **786 passed**；旧协议/源文件校验通过。
- 流程已启动，会话 80842。第一对两个模型实际配置已核对；初始策略张量摘要
  都是 `42b82abffe346062bb2af148705597429f09e1fb8e9a13acdd83d14ad114d88e`，
  初始四家族共 400 条验证记录逐局一致。现正训练，不是完成结果。

后续自动流程：按三对完成训练，保存每个准确验证权重；冻结所有最终/所选模型，
先全部验证重载再做诊断测试并汇总。最终固定预算权重是主比较，中途最佳是辅助。
五个测试池来自已分析过的上一确认，只换新局种子，不能冒充全新独立泛化证据。

流程正常结束后还需重计数据、核对实际权重/预算、审查最多 18 个最终模型回放，
再给简明结论；不以绿色测试、程序完成标记或一个好种子宣布稳定 recipe。
若子进程失败或超过每对一小时上限，停止并保留数据，无自动重试或扩预算。
没有启动 GPU/集群、发布、网站替换或旧实验清理。

已复用本对话原有的 SAP 完成通知，更新为“SAP 奖励成本对照完成复核”
（监控 ID `sap`，每 15 分钟检查）。正常运行无可处理变化时静默；完成后继续
本轮已批准的只读计数/最多 18 回放审查并报告，出错则报告且不重启训练。
结果或失败通知后暂停监控。此跟进需要本机和桌面应用保持运行。

## 完成复核

2026-09-08（本地晚间）：六模型均跑满 2,097,152 步，总 12,582,912。
全部 36 个验证权重的实际策略张量、步数、环境、PPO 参数及训练配置核对一致，
每对初始策略/400 局验证相同，三个初始策略摘要互异。所选及最终权重均与
对应准确验证权重一致；保留最终更新前后同一步数的区别。

14,400 条验证曲线、4,800 条重载、12,000 条诊断测试逐局重计一致；模型/选择
绑定、各家族和宏平均、对手回退、逐家族配对区间和全部比较摘要重算一致。
`runs/reward-cost-pilot-audit-v1` 精确复现 13 个最终模型验证回放，未超出 18 局
上限；1,294 个动作各按本臂真实成本核对奖励和 value 对照摘要。该审查自身
未读测试，其 held_out_tests_opened=false 不表示主实验未运行测试。

最终 B 三种子空队失败均为 0，最差测试家族强制率为 12%/14%/12%，
学习型十胜率全部为 0。第三对平均胜场略降，因此候选筛查未通过。
所有最终模型零截断；B/203201 所选更新前版本有一个真实 turn_limit 截断，
报告已单独披露。没有重选检查点、重启训练、修改门槛或批准长确认。

完成复核记录：`runs/reward-cost-review-v1.json`。报告就绪后，本对话监控 `sap`
已通过应用正式暂停，避免重复通知；尚未解决的整体稳定性目标没有标为完成。
