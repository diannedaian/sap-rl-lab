# 下一层：50宠物、普通Tier4商店（v6）

2026-09-14，用户批准扩下一层并按原recipe开始有界训练。
本轮不是旧80%验收的重试；旧结果仍为未通过，旧模型不覆盖。

## 实施顺序和边界

1. 独立v6目录与catalog：普通商店Tier1–4，完整Tier5升级奖励依赖。
   目标仍锁定游戏0.46，不把最新版数据混进旧规则。
2. 新增十只Tier5宠物、Chick/Milk依赖、Tier4的Bread/Pear/Canned Food。
   新增状态必须可观察、可序列化；先逐机制和交互测试，再压力/回放/重载测试。
3. 保留小MLP、BC20轮、Maskable PPO、KL target 0.01、LR 3e-4、
   gamma1、GAE0.95、entropy0、clip0.2、10 epochs、8环境×256、batch256。
   每动作扣0.005，30动作强制开战；不同时调奖励/网络。
4. 因新增物种/食物/状态导致输入合同改变，本轮使用新环境老师数据重新BC。
   不把不兼容的旧40宠物权重硬加载，也不声称检验了旧权重迁移。
5. 三种固定脚本老师（数值/召唤/节奏），收集全部示范、不按输赢过滤。
   BC训练每老师600局，独立验证每老师100局；教师是启发式，不称专家。
6. 新环境先生成独立历史学习型对手：3个训练族、2个验证族、2个测试族，
   每个独立初始化、BC后至多1,048,576步PPO，固定终点生成快照。
   三个候选独立初始化BC后各至多2,097,152步，训练对手保持
   3脚本+3学习型等权（50/50）。最大PPO总计13,631,488步；无自动追加。
   单机CPU，至多2个worker，整条训练流水线最多4小时，任何异常停下汇报。
7. 新验证选择以两个学习型验证族平均十胜率为主，脚本族保留可靠性和回归检查。
   固定预算与BC起点均报告；选择完并精确重载后才开启测试。
   新测试对手由独立生成模型产生，不仅是旧池换局seed。
8. 报告BC→PPO增益、各对手族、强制/截断/空队失败、动作/新机制覆盖和回放。
   不要求新难度直接80%，不直接扩Tier5普通商店/完整60宠物。

训练门：规则测试、可达轨迹回放与观察mask一致性、BC/小PPO精确保存重载。
若不能通过，不绕过门槛启动大训练。社区资料与官方客户端实证分开标注。
没有授权GPU、上传、发布、删除旧文件或修改旧报告。
