# 2026-09-15：只继续评估，不追加训练

用户在怀疑合盖中断后要求检查并继续。检查发现：7个对手生成器和3个候选均
完成预定PPO预算，总计13,631,488决策。2026-09-14 13:33的退出是
seed54301没有合格验证检查点，而非训练中断或四小时超时。
原计划、代码、数据、权重及失败记录不修改，缺失选模不能用固定终点代替。

本次仅补完原计划中的评估与诊断：

1. 核验全部生成器预算、3×9个候选检查点、原选择规则、更新日志及哈希。
2. 在测试开启前绑定每支BC起点、固定训练终点，以及原规则实际选出的模型。
   相同权重只计算一次，再以别名引用。54301的selected保持null，不剔除这个seed，
   不将其终点冒称selected，不报告仅两个成功选模的均值作为三支结果。
3. 原验证集每族200局，所有绑定模型逐局精确重载后才开启原测试集每族500局。
   验证/测试仍为原5个族、原seed和原模型，测试不选模、不调奖励或门槛。
   当前为6个独特模型：共6,000重载验证局、15,000测试局；最多9模型的原预算。
4. 每个模型从原验证轨迹选择最早失败、强制开战、成功各一例，去重，最多18局。
   保存动作概率、价值和奖励并核对原逐局记录；是诊断样本，不用于估计发生率。
5. 新目录`runs/tier4-evaluation-v1`保存评估封存、结果、日志和失败诊断。
   本机CPU最多两个worker，一小时硬上限；新增PPO训练决策为0。
6. 原实验保持选模不完整/可靠性未通过结论。完成评估不等于通过原门槛，
   不覆盖旧交付，不自动重训或扩到60只；报告强度与可靠性两个维度。

原最终验证学习型十胜率为40.00%/46.25%/44.75%，不是测试结果。
54301终点在一个验证族有12/200局强制开战（6%），超过原5%上限；
54101终点多个族超限，原选择退回BC；54201终点合格。
全部验证记录没有截断或不买宠物零胜失败。不得归因于合盖或据此声称完全不循环。
