# 80%轮动作统计勘误（测试前）

2026-09-14，流水线刚启动、首两支约25万步时，查看真实更新日志发现：动作枚举为 `toggle_freeze`，新封存编排文件 `kl_80_experiment.result_metrics` 的代理统计误用了 `freeze`。新单元测试的模拟字段也用了同一个错误名称，未捕获真实数据接口差异。

影响仅为新增报告中的 `(换位+冻结)/战斗` 代理指标漏算冻结，可能误判行为是否退步。**训练器、采样、奖励、KL、验证选模、十胜率和强制率不受影响。** 没有重启、补训或修改任何封存文件，没有查看本轮测试结果。

保持原代码和协议作为历史证据，不在运行中改变源码哈希。新增独立复核脚本 `scripts/finalize_kl_80.py`，从全部原始新测试逐局记录按真实 `ActionKind.FREEZE.value`（`toggle_freeze`）重新计算该代理指标，再使用原封存计划完全相同的验收门槛。用真实枚举字段和一个“漏算则通过、正确统计则不通过”的回归测试验证。

最终条件分支以 **`runs/kl-80-v1/final_review.json`** 为准，不能直接依据原 `summary.json/acceptance` 宣布通过。该文件只能在原流水线和回放审计全部完成后写入，包含原始/修正指标、原始文件与修正脚本/勘误的哈希。仍需人工检查回放，不自动修改旧交付或启动新Tier训练。

这属于纠正测量实现，不改变80%目标、可靠性门槛、训练预算、模型选择或测试次数。该勘误在测试前落盘，最终报告明确披露。
