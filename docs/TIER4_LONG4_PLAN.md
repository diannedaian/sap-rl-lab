# Tier4第四段有界续训

2026-09-16，用户批准再跑一轮，检查相同配方继续训练的收益。不是自动训练到80%，不扩宠物。

- 起点：`runs/tier4-long3-v1`三支固定训练终点54101/54201/54301，沿用统一规则，
  不依据已见测试挑起点。C的旧验证选模中间点及其他历史模型全部保留。
- 每支追加1,048,576步，总新增3,145,728步。源检查点本段计数1,048,576；
  源谱系累计5,242,880，新终点谱系累计6,291,456步。
- 完整actor/critic权重热启动、新优化器，采样seed58101/58201/58301；
  不重做BC，不冒称恢复旧Adam/RNG。开始优化前逐局重现旧终点，不符则零更新退出。
- 配方保持：KL target0.01，近似KL超过0.015时停止当前rollout的后续优化；
  小MLP、LR3e-4、gamma1、GAE0.95、entropy0、clip0.2、10epochs、8环境×256、
  batch256、每动作−0.005、30步强制战斗。环境、物种、奖励、网络、对手池和混合权重不变。
- 原5个验证族各200局、seed59000000，每262,144步评估；学习型优先选模，
  每族强制局率≤5%、无截断等原门槛不变。没有合格检查点保持null，不冒充通过。
- 封存每支起点/固定终点/实际验证选模，重复检查点以别名去重；全部精确重载
  后才开新测试episode seed71000000，每族500局，最多9模型/22,500测试局。
  这是既有独立对手池的新对局，不是新对手类型；反复研究同一生态的限制仍在。
- 主比较为三支固定终点相对各自起点的学习型十胜率，逐支及均值都报告；
  另报验证选模成绩，两种口径不混用。上轮验证选模均值79.87%、固定终点80.6%。
  同时报强制局率、截断、不买宠物零胜失败、选模覆盖与最多27个诊断回放。
  不以测试选模，不只报最好seed，不保证80%，不把预算完成当作收敛证明。
- 新目录`runs/tier4-long4-v1`；CPU最多两个worker、单模型1小时、整轮2小时
  硬上限。异常停止，无自动追加/重跑，无GPU、上传、扩Tier或旧文件覆盖。

目标是检查增益是否趋缓、不同seed是否一致，并确认可靠性不恶化。
