# KL 刹车夜间状态

2026-09-12 夜间（纽约）。用户已授权本轮正式配对继续训练。

## 当前状态（2026-09-13 12:45 UTC，优先于下方启动历史）

用户提前醒来，本次对话回复交付简洁晨报及`docs/KL_BRAKE_REPORT.md`链接；现有`sap`夜间监控已通过应用工具确认暂停，不再等09:00重复通知，不归档任务。没有新增训练、评估、推送或模型替换。更广泛的长期训练稳定性仍未证实，后续实验等待用户决定。

## 评估完成记录（2026-09-13 05:02 UTC）

**恢复评估已完成，报告已准备，尚未发送晨报。** 会话35771已正常结束，不再轮询；原19206仍为失败历史，不重启任何流程。新目录有pipeline_complete、summary及audit/summary，34个worker退出码均0；恢复耗时445秒，无新增训练。

`docs/KL_BRAKE_REPORT.md`为简明晨报，`docs/KL_BRAKE_EVIDENCE.md`保留计数和限制。三对共同步数测试：学习型十胜率原PPO56.25/63.5/63.5%→KL65/71/72.5%；强制9.1/13.7/0.1%→0.1/0.2/0%。平均61.08→69.50%，7.63→0.10%。但验证所选模型只2/3对改善，第三对KL67%低于原PPO71.5%；回放仍见KL循环。不能说稳定性已彻底解决。

完整核验：原/恢复协议及旧最佳哈希、三对共同权重、所有6400局重载精确、所有重载先于测试；16000测试逐局独立复算；恢复审计41600行及17回放2205动作，奖励公式核验通过。只读补充校验会话70833已结束。下一次定时检查只需看报告和到期时间，不重复重载、测试、审计或训练。

原计划在2026-09-13 13:00 UTC（纽约09:00）发送晨报；用户提前回来，已改为在本次对话交付并暂停监控。勿重复发送。

## 中断与恢复历史

用户说明刚更新Codex并于04:29 UTC左右授权继续。原流水线19206已终止，工具不再认识这个会话；不要再轮询/重启。原`runs/kl-brake-v1/pipeline_failed.json`保留：47分50秒后worker非零退出；五支各2,097,152步完成，control-204201记录到1,839,104步，最近权重1,835,008步。没有证据证明PPO数值错误；更新导致输出管道断开只是推测，原完整退出堆栈未落盘。

只读复核通过：53份验证权重绑定、21,200条原验证记录、配置/起点/源码/对手/旧最佳哈希；已记录总训练12,324,864步。原流程未选模、未开测试，不能称原固定预算完成。

已按用户最新授权启动**仅评估的恢复流程**：会话 **35771**，`runs/kl-brake-recovery-v1`，计划`docs/KL_BRAKE_RECOVERY_PLAN.md`。六模型按共同最大可用保存点1,835,008步比较，次要候选只从此前前缀选；这是中断后修订，不是原预注册的完整209.7万步对照。新增PPO/BC训练为0；旧文件不动。33项相关测试通过（新增恢复5项），ruff通过；48份前缀权重与19,200验证局准备校验通过，15个候选已复制，历史最佳验证后共冻结16个比较。

恢复流程最多两CPU worker、单Torch线程、2小时上限；最多400历史参考验证、6,400重载、16,000测试、18回放。全部重载精确才开测试。完整worker日志和退出码保存到新目录，终态看新目录`pipeline_complete.json`/`pipeline_failed.json`。不要再启动任何训练或重复恢复流程。准备会话46630、测试97263、只读校验14018/98759均已结束。

晨报仍为今天纽约09:00（13:00 UTC），请报告原流程中断与修订评估结果两部分，提前结束则安静准备。OpenAI Docs确认本地定时任务需要电脑和app保持运行；已据此保留本任务晨报跟进，不新建重复任务。

## 原始启动记录

- 冻结计划：`docs/KL_BRAKE_PLAN.md`；输出 `runs/kl-brake-v1`，协议和来源权重校验通过。
- 已启动的唯一流水线会话：**19206**。不要重复启动；先读会话及目录内真实进度。
- 启动核验：第一对17301已分别记录68/81批真实更新，初始权重哈希完全相同；不是仅创建计划。晨报监控`sap`已更新为本轮并启用，每15分钟检查，在到期前无异常则安静。
- 6 模型×2,097,152 决策，上限12,582,912；最多两个CPU worker、单Torch线程；流水线4小时上限，每模型1小时软上限。已用有限时长的防空闲休眠；不能阻止合盖/断电。
- 唯一算法差异为 `target_kl=None` / `0.01`，同来源同新seed，权重初始化、新优化器。普通退化不提前停；截断/异常/超时停止且不自动重试。
- 已完成全部现有测试与10项编排测试（823项），随后新增汇总边界测试并通过全部11项编排测试，共824项独立测试已覆盖；ruff与diff检查通过。只有既有Matplotlib弃用警告。
- 训练完成后流水线会自动冻结所有比较、全部重载、全部测试、复算计数和最多18回放。`summary.json`先于审计产生；必须同时有`audit/summary.json`及`pipeline_complete.json`才能称全流程完成。失败看`pipeline_failed.json`和各worker日志/`failed.json`。
- 晨报到期：**2026-09-13 09:00 America/New_York / 13:00 UTC**。输出`docs/KL_BRAKE_REPORT.md`，在本任务给简洁中文报告；到期未完成也报告真实进度。正常运行不发重复通知。
- 不调参、不追加训练、不覆盖/清理历史、不推送、不发布、不用GPU。保护旧最佳哈希：`094f5f2ca802f98ae5ca7a9481d6c168d11e0c3853774cbd65e9f97c0866e644`。
- 历史完成会话12464、44858、73948、14044、77798、2849、16036，以及本轮测试39832/10879、准备35383/96092均已结束，不再轮询。监控只跟进19206和新流水线。

## 晨报要回答

训了多少、从哪里继续、唯一变化是什么；每对末尾学习型对手十胜率/强制率与验证所选版本；是否真的比初始更强；实际KL与梯度步数是否下降；冻结/换位循环、购买、剩余金币和价值估计的回放证据。没有改善就明确说无效，不把能选出好检查点当成训练稳定，不把新局seed当成新对手生态。当前仍40宠物、普通商店Tier1–3、Tier4仅升级奖励。

## 静默巡检

2026-09-13 03:54 UTC：会话19206仍在运行；17301两支各2,097,152步完成，均为budget_complete；204101两支正在训练，204201尚未开始。未见失败标记，冻结输入和历史最佳哈希再次核对通过。尚未选模/测试，不作效果结论；距晨报到期仍早，无需用户处理。只读校验会话13795已结束。

2026-09-13 04:10 UTC：17301和204101共四支均完成各2,097,152步，均为budget_complete；最后一对204201正在训练。会话19206运行正常，无失败标记，尚未进入选模/测试；继续静默监控，不追加任务。
