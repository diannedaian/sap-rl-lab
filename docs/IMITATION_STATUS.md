# BC → PPO 夜间实验状态

**2026-09-09 09:35 EDT：本次晨间回复交付报告，夜间自动任务 `sap` 已确认暂停。** 完成记录、报告、独立复核及旧最佳权重哈希再次核对一致；没有重新训练或追加评估。发送记录见 `runs/imitation-v1/morning_report_delivery.json`，实际交付以本次对话最终回复为准。以下等待发送说明为历史记录，不再据此重复通知或启动实验。

**2026-09-09 03:04 EDT：完整流水线及独立证据复核已完成，晨间报告已写好，等待 09:00 EDT 发送。** 报告 `docs/IMITATION_REPORT.md`。会话 **12464 已退出 0，不再轮询或重启**；只读复核会话 **44858 也已退出 0**。无运行中训练、无失败标记、无追加预算。`pipeline_complete.json` 记录耗时 7,430.63 秒；`evidence_review.json` 记录 63 份 BC /60 份 PPO 验证权重、24,000 曲线评估和22,400 重载/测试逐局记录核对通过；自动审计完成25回放 /3,967动作。旧最佳哈希未变。

核心结论：BC-only 学习型对手十胜率平均 2.58%，经 PPO 的 final 为60.33%，从零 final 为55.08%；但只有1/3配对提升，BC final强制局率99.5%/60.9%/4.5%，后期循环没有解决。验证所选 BC-204201 有74%十胜、1/1,000强制；从零对照也有72.5%，不宣称BC普遍更好。完整细节及随机流复现 caveat见报告。

**后续心跳只需确认完成/报告文件还在并读取时间，不重新训练、评估、审计或轮询已结束会话。** 到2026-09-09 13:00 UTC或之后首次运行时，发送一次简洁中文晨报并链接报告，然后暂停自动任务 `sap`。此前保持安静。不要将未解决的更广泛稳定性目标标为完成。

2026-09-09 00:40 EDT：实现完成、正式流水线已启动，当前先生成示范，尚未声称 PPO 已开始或效果改善。

**00:41 EDT 更新：示范与三份 BC 已完成，全部通过预定闭环门，第一对 PPO 已开始。** 训练示范 1,800 局 /124,809 个动作；监督验证 300 局 /20,752 个动作。三个种子选中监督验证 loss 最低的 epoch 20，动作标签准确率约 67.8–69.3%。在各自 400 局闭环验证里，平均胜场从随机初始的 0.00/0.028/0.020 升至 4.388/4.595/4.333；三者未观察到不购买零胜失败或截断，但最差对手族仍有 9%/7%/10% 的强制开战局。说明基础模仿有作用，不代表可靠性或十胜实力已经达标。`scratch-seed17301` 与 `bc-seed17301` 正在训练，PPO 对照结果尚未完成。

- 已验证：新增 10 项测试；全套 796 项通过；新增源文件和审计脚本 lint 通过。测试包含两个 32 步 PPO 接线烟测（共 64 步，临时目录，不属于科学实验），检查 actor 更新、critic 不变、随机初始化与原训练器一致、保存重载、初始化参数绑定和同一步更新前后验证分别保存。测试中的小型 BC 仅为接线检查，不作为性能证据。
- 正式实验：`runs/imitation-v1`；方案：`docs/IMITATION_PLAN.md`。协议 SHA256：`b12987be07e3d50f2cdc82226139b0df8d27e208b571264793794d8fc8bf3df5`。
- 本次拥有的执行会话：**12464**。初始输出 `Starting demos: all`。不要重复启动已有流水线或恢复历史训练。
- 有限防空闲休眠仅用于这次工作，最长六小时；仍需电脑供电、应用运行，合盖/手动睡眠可能中断。未修改系统长期设置。
- 三教师各 600 训练局 +100 监督验证局。三个 BC 初始化先过闭环基础能力门；通过才运行六个 PPO 模型，每个 8,388,608 决策，共最多 50,331,648，最多两个 Torch 单线程 CPU 工作进程。BC 计算成本另计。
- PPO 两组奖励均为原游戏奖励减每动作 0.005；没有新增购买奖励、成功奖励或冻结惩罚；只对照初始化。普通商店仍到 Tier 3，Tier 4 为升级奖励，并未再扩大规则。
- 旧最佳 SHA256 已确认并复制到新目录作只读比较；原文件、全部旧结果和网站不改。

## 自动阶段及终止记录

`pipeline_started.json` → `demonstrations.json` → 三份 `clone-seed*-complete.json` → `bc_gate.json`。

- 如果门未通过：`stopped_at_bc_gate.json`，PPO 步数为零；自动生成 `audit/summary.json` 中的代表性 BC 回放，不自动修门或加训。
- 如果通过：六份 `*_complete.json` → `selection.json`（冻结 16 模型）→ 全部 `reload-*.json`（6,400 局精确重载）→ 全部 `test-*.json`（16,000 局诊断测试）→ `summary.json` → `audit/summary.json`（BC-only 与 PPO 最终模型最多 27 回放）→ `pipeline_complete.json`。
- 异常/六小时超时：`pipeline_failed.json`，保留所有数据；停止，不自动重试、修改冻结源码、改奖励/阈值、追加预算。
- 只有 `pipeline_complete.json` 才表示完整流水线成功结束。`summary.json` 内 `replay_audit_pending: true` 是产生时的不可变状态，须另读后续 `audit/summary.json` 和完成记录判断审计是否已经完成。

## 晨间报告待办

正式运行后不要编辑被冻结的 `src`、计划或审计脚本。可只读核查、写新的分析产物和更新此状态/新增 `docs/IMITATION_REPORT.md`。

跟进会话及阶段文件；不明状态先检查，不重复启动。完成后复算逐局 metrics 和对手回退、每个 seed 的 final/best，核对精确 warm-start、BC critic 不变、PPO 实际配置/步数与所有验证权重绑定；审阅生成回放，说明 BC-only value 从未训练，不能用于解释已学价值。

明早 **2026-09-09 09:00 America/New_York（13:00 UTC）** 报告；在此之前正常运行保持安静。若异常需要用户操作，及时说明。即使未完，也在晨间给实际已完成/未完成情况。结果不是预先保证：不把 BC loss 下降等同于游戏能力提升，不把 PPO 运行完等同于增强，不把复用对手池的诊断结果称为独立泛化确认。

报告讲清：教了什么、没教冻结、示范/监督学习/PPO 的不同、各自实际数据与计算量、是否忘掉基础行为、是否比同预算随机起点强、是否超过 BC-only 和旧最佳。保留旧最佳，未经用户决定不替换交付模型或发布。
