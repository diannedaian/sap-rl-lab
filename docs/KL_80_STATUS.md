# KL 80% 验收状态

## 2026-09-14：已完成，未过80%，停止

流水线36分32秒完成，主会话54391退出0，全部24个worker正常退出。独立复算27个权重/36,900逐局记录，确认所有重载先于测试、输入和旧最佳哈希不变；14回放/1,448动作已核对并检查真实动作、概率与价值。`final_review.json`已生成，冻结动作计数勘误已纳入。

主要验收三支验证选模70.3% /76.6% /82.8%，合计2,297/3,000=76.57%，未达2,400局；固定终点平均78.87%亦未过线。可靠性和修正后行为代理均通过，但仍有局部循环。未追加训练、未准备Tier扩展、未替换任何旧交付。

结果：[KL_80_REPORT.md](KL_80_REPORT.md)；证据：[KL_80_EVIDENCE.md](KL_80_EVIDENCE.md)。主会话和所有训练均已结束，不再轮询或重启。以下为启动历史，不代表仍在运行。

本轮自动跟进`sap-80`已通过应用删除（返回`deleteStatus=deleted`），未归档本对话。

## 启动记录

2026-09-14 13:04 UTC（纽约09:04）：已启动 `runs/kl-80-v1`，训练、封存、精确重载、新测试和回放审计由同一有界流水线顺序执行。尚无本轮测试结果。

**启动期统计勘误**：真实冻结动作键是 `toggle_freeze`，封存新报告中的行为代理误用了 `freeze`，漏算冻结。不影响训练/十胜率/选模。[测试前勘误](KL_80_METRIC_ADDENDUM.md) 已落盘；原代码不改、不重训。原流水线完成后必须先运行只读复核 `scripts/finalize_kl_80.py --output runs/kl-80-v1`，以生成的 `final_review.json/acceptance` 作为条件分支依据，不能用未修正的summary判定扩展。该脚本不新增训练或评估局数。

- 本轮主会话：`54391`。启动时间见 `runs/kl-80-v1/pipeline_started.json`。
- 三支 KL 各追加2,097,152决策，总上限6,291,456。最多两支本机CPU并行；不使用集群/GPU。
- 新增15项无预算测试；全项目858项测试通过，相关46项通过，新增文件Ruff通过。旧训练器、规则、奖励、旧最佳未改。
- 统计勘误另加4项真实动作枚举回归测试；修订后全项目862项测试通过，复核脚本Ruff通过。修订仅新复核脚本/测试/勘误文档，封存输入哈希再次通过，不影响当前训练。
- 起点为上轮三支固定终点，复制权重、新优化器/RNG；不是精确恢复历史进程。
- 预声明：[KL_80_PLAN.md](KL_80_PLAN.md)。该计划和代码已封存，运行中不改动。
- `summary.json` 的 acceptance 为待审计判定；只有 `pipeline_complete.json` 和 `audit/summary.json` 齐全且独立核验后才能宣布完成。
- 主要验收：三个验证选模在两个学习型对手族的新3,000局测试上，至少2,400局十胜。原始整数判定，不按显示四舍五入。
- 达标且可靠性/行为审查通过：写简洁报告，并准备下一层Tier的规则依赖/证据/测试计划；不启动新Tier训练、不修改现有冻结沙盒。
- 未达标、退步或不完整：停止汇报，不重跑、不扩大预算、不换测试、不以测试选择其他检查点。
- 结果报告预定 `docs/KL_80_REPORT.md`；证据补充 `docs/KL_80_EVIDENCE.md`。没有自动替换任何旧交付。

## 后续检查

本轮日志在 `runs/kl-80-v1/pipeline.log`、`worker-*.log`、各支 `updates.jsonl`。有 `pipeline_failed.json` 则读完整堆栈和本轮worker退出码，停止并报告，不能猜原因或悄悄补预算。所有结果写入均拒绝覆盖。

自动跟进 `sap-80` 已启用，每15分钟检查一次；正常运行时安静，完成/失败/需要用户行动才通知。只在需要时检查本轮会话54391，不轮询历史已完成会话，不启动重复流水线。任务完成并交付（若过线则包含扩展准备）后删除本次跟进，不归档当前对话。
