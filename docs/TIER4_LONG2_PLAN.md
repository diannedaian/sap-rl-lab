# Tier4第二段续训：保持KL和原配方

2026-09-16，用户批准三支各再训约105万步，并确认KL继续使用。

- 起点：`runs/tier4-long-v1`三支固定终点（54101/54201/54301），均保留，
  不按已见测试分数挑选起点。上一段本地计数1,048,576，谱系累计3,145,728步。
- 每支再追加1,048,576步，总新增3,145,728步；谱系终点为4,194,304步。
  不重做BC；同此前续训方式，actor/critic权重复制，优化器重新初始化，
  采样seed56101/56201/56301；不是旧优化器/RNG的精确恢复。
- KL target=0.01，继续使用PPO近似KL早停（超过1.5倍target停止当前批后续优化），
  不是加入相对BC老师的固定KL损失，也不是KL达到0.01就永远终止训练。
- 网络、环境、奖励、训练/验证/测试对手池、3脚本+3学习型等权混合全部不变。
  LR3e-4、gamma1、GAE0.95、entropy0、clip0.2、10epochs、8环境×256、batch256；
  每动作−0.005、30动作强制开战。保留每族强制局率≤5%等原验证筛选门槛。
- 每262,144步验证，仍为原5族各200局、seed59000000。开始优化前必须逐局
  重现上一段终点，失败则零更新退出。无合格选模保持null，继续诊断而不冒充通过。
- 测试前封存起点/固定终点/实际验证选模，重复检查点用别名去重；全部精确重载
  完成后才跑新测试episode seed69000000，每族500局，最多9模型/22,500测试局。
  沿用既有独立对手池的新对局，不是新对手类型，反复研究此生态的局限仍在。
- 主要比较：三支固定终点相对起点的学习型十胜率变化；逐支和均值同时报告。
  同时报告强制局率、截断、空队失败、选模覆盖及最多27个诊断回放，尤其检查C。
  不以测试选模，不仅报告最好一支，不以总平均掩盖不合格seed，不承诺80%。
- 本机CPU最多两个worker，单模型1小时、整轮含评估2小时硬上限，无自动追加。
  新目录`runs/tier4-long2-v1`；旧代码、模型、报告不覆盖，不上传、不扩宠物数。

问题仍是：增加一段预算能否继续提高强度，并改善/维持可靠性？
停止原因、固定终点和验证选模分别记录；不将预算结束写成“已证明收敛”。
